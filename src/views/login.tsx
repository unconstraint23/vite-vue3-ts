import { defineComponent } from 'vue'

export default defineComponent({
    props: {},
    emits: [],
    components: {},
    setup(props, ctx) {
        return () => <div>
            <el-button type="primary">
                登录页
            </el-button>
            
            
            </div>
    }
})